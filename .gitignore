# Hugo build output
public/
resources/_gen/

# Node (the routing Worker's dev dependencies, and the shared brand theme the
# site build now installs from npm rather than carrying as a submodule)
node_modules/

# OS files
.DS_Store
Thumbs.db

# Editor
*.swp
*.swo
*~
.idea/
.vscode/

# Hugo lock
.hugo_build.lock

# Ignored Files
nogit/
CLAUDE.md

# Wrangler scratch from `wrangler dev`
/worker/.wrangler/
