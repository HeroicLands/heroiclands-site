---
title: Blog
description: Occasional thoughts on game design, worldbuilding, and development.

# The hero image every post in this section carries.
#
# The shared theme derives a page's hero from its `type:`, so a post
# (`type: blog-post`) asked for `images/banners/blog-post.webp` — an image
# that has never been drawn, and 404s. The band still rendered full height
# with its title and gradient, so the page read as a blank slab
# (heroiclands-site#19).
#
# The theme is not the place to fix it: it carries layout, not addresses, and
# which image a section of *this* site wears is this site's to say. The brand
# banner is the address already chosen for the two other heroes configured by
# hand here (`params.home.heroimage` and `params.notfound.heroimage` in
# `hugo.toml`), so the blog reads as part of the same site rather than as a
# page wearing a stand-in. Point this at `banners/blog-post.webp` the day one
# is drawn.
cascade:
    banner: brand/sohl-banner.webp
---
